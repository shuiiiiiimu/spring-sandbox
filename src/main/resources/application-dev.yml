hello:
  name: spring
spring:
  datasource:
    url: jdbc:mysql://mysql_server:3306/sandbox
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    database: MYSQL
    hibernate:
      ddl-auto: update #create-drop
rabbitmq:
  host: mq_server
  port: 5672
  username: guest
  password: guest
  virtual-host: /
  app:
    routing_key: sandbox
